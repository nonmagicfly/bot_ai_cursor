# 📊 Grafana Dashboard - Визуализация метрик бота

## Обзор

Дашборд "Telegram Bot - Мониторинг" предоставляет полную визуализацию всех метрик бота из Prometheus.

## Доступ к дашборду

1. Откройте Grafana: `http://ваш_ip:3000`
2. Войдите (логин: `admin`, пароль: из `.env`)
3. Дашборд должен автоматически загрузиться из `grafana/dashboards/telegram-bot-dashboard.json`

Если дашборд не появился автоматически:
- Перейдите в **Dashboards** → **Browse**
- Найдите "Telegram Bot - Мониторинг"
- Или создайте новый дашборд и импортируйте JSON файл

## Панели дашборда

### 1. Статус бота
- **Тип**: Статистика
- **Метрика**: `bot_health`
- **Описание**: Показывает текущий статус бота (✅ Работает / ❌ Не работает)

### 2. Всего пользователей
- **Тип**: Статистика
- **Метрика**: `bot_users_total`
- **Описание**: Общее количество зарегистрированных пользователей

### 3. Активных сегодня
- **Тип**: Статистика
- **Метрика**: `bot_users_active_today`
- **Описание**: Количество пользователей, которые использовали бота сегодня

### 4. Всего программ
- **Тип**: Статистика
- **Метрика**: `bot_programs_total`
- **Описание**: Общее количество созданных программ тренировок

### 5. Активных программ
- **Тип**: Статистика
- **Метрика**: `bot_programs_active`
- **Описание**: Количество активных (неархивированных) программ

### 6. Записей сегодня
- **Тип**: Статистика
- **Метрика**: `bot_records_today`
- **Описание**: Количество записей тренировок, сделанных сегодня

### 7. Операции по типам (сегодня)
- **Тип**: График (столбчатая диаграмма)
- **Метрика**: `bot_operations_today{operation_type}`
- **Описание**: Визуализация операций по типам за сегодня (start, newprogram, startworkout, etc.)
- **Формат**: Стекированная столбчатая диаграмма

### 8. Всего операций (счётчик)
- **Тип**: График временных рядов
- **Метрика**: `rate(bot_operations_total[5m])`
- **Описание**: Скорость выполнения операций (операций в секунду)
- **Формат**: Линейный график с разбивкой по типам операций

### 9. Длительность обработки запросов
- **Тип**: График временных рядов
- **Метрики**: 
  - `histogram_quantile(0.50, ...)` - медиана (p50)
  - `histogram_quantile(0.95, ...)` - 95-й перцентиль (p95)
  - `histogram_quantile(0.99, ...)` - 99-й перцентиль (p99)
- **Описание**: Время обработки запросов по обработчикам
- **Единица**: секунды

### 10. Ошибки по типам
- **Тип**: График временных рядов
- **Метрика**: `rate(bot_request_errors_total[5m])`
- **Описание**: Скорость возникновения ошибок по типам
- **Единица**: ошибок в секунду

### 11. Использование CPU
- **Тип**: График временных рядов
- **Метрика**: `bot_system_cpu_percent`
- **Описание**: Загрузка процессора в процентах
- **Цветовая индикация**:
  - 🟢 Зелёный: < 50%
  - 🟡 Жёлтый: 50-80%
  - 🔴 Красный: > 80%

### 12. Использование памяти
- **Тип**: График временных рядов
- **Метрика**: `bot_system_memory_percent`
- **Описание**: Использование памяти в процентах
- **Цветовая индикация**:
  - 🟢 Зелёный: < 50%
  - 🟡 Жёлтый: 50-80%
  - 🔴 Красный: > 80%

### 13. Время работы бота
- **Тип**: Статистика
- **Метрика**: `bot_uptime_seconds`
- **Описание**: Время непрерывной работы бота
- **Формат**: Автоматическое форматирование (секунды/часы/дни)

### 14. Топ операций за сегодня
- **Тип**: Таблица
- **Метрика**: `topk(10, bot_operations_today)`
- **Описание**: Топ-10 самых частых операций за сегодня
- **Формат**: Таблица с типами операций и их количеством

### 15. Статистика по операциям
- **Тип**: Столбчатая диаграмма
- **Метрика**: `sum by (operation_type) (bot_operations_total)`
- **Описание**: Общее количество операций каждого типа за всё время
- **Формат**: Горизонтальная столбчатая диаграмма

## Настройка дашборда

### Обновление данных
- **Автообновление**: Каждые 10 секунд
- **Диапазон времени**: Последние 6 часов (по умолчанию)
- Можно изменить в правом верхнем углу дашборда

### Изменение временного диапазона
1. Нажмите на селектор времени в правом верхнем углу
2. Выберите нужный диапазон:
   - Последний час
   - Последние 6 часов
   - Последние 24 часа
   - Последняя неделя
   - Или задайте кастомный диапазон

### Экспорт данных
1. Нажмите на иконку ⚙️ (Settings) в правом верхнем углу
2. Выберите **Export** → **Save to file**
3. JSON файл будет скачан

### Создание алертов
1. Нажмите на панель, для которой хотите создать алерт
2. Выберите **Edit**
3. Перейдите на вкладку **Alert**
4. Настройте условия алерта:
   - Например: CPU > 80% в течение 5 минут
   - Или: Количество ошибок > 10 в минуту

## Полезные PromQL запросы

### Количество операций за последний час
```promql
sum(increase(bot_operations_total[1h]))
```

### Средняя длительность обработки запросов
```promql
rate(bot_request_duration_seconds_sum[5m]) / rate(bot_request_duration_seconds_count[5m])
```

### Процент ошибок
```promql
rate(bot_request_errors_total[5m]) / rate(bot_operations_total[5m]) * 100
```

### Активность пользователей по часам
```promql
sum by (hour) (increase(bot_operations_total[1h]))
```

## Troubleshooting

### Дашборд не загружается
1. Проверьте, что Grafana имеет доступ к Prometheus:
   - Configuration → Data Sources → Prometheus
   - URL должен быть: `http://prometheus:9090`
   - Нажмите **Test** для проверки

2. Проверьте, что дашборд находится в правильной директории:
   ```bash
   ls -la grafana/dashboards/telegram-bot-dashboard.json
   ```

3. Перезапустите Grafana:
   ```bash
   docker-compose -f docker-compose.yml -f docker-compose.monitoring.yml restart grafana
   ```

### Метрики не отображаются
1. Проверьте, что бот экспортирует метрики:
   ```bash
   curl http://localhost:8000/metrics
   ```

2. Проверьте, что Prometheus собирает метрики:
   - Откройте Prometheus: `http://ваш_ip:9090`
   - Перейдите в **Status** → **Targets**
   - Убедитесь, что `telegram-bot:8000` в статусе **UP**

3. Проверьте логи Prometheus:
   ```bash
   docker-compose -f docker-compose.yml -f docker-compose.monitoring.yml logs prometheus
   ```

### Графики пустые
1. Убедитесь, что бот работает и генерирует метрики
2. Проверьте временной диапазон (возможно, данные ещё не появились)
3. Проверьте, что метрики имеют правильные имена (см. `metrics.py`)

## Дополнительные ресурсы

- 📖 [MONITORING.md](MONITORING.md) - Документация по мониторингу
- 📖 [Prometheus Query Language](https://prometheus.io/docs/prometheus/latest/querying/basics/)
- 📖 [Grafana Documentation](https://grafana.com/docs/grafana/latest/)

