# 🔐 Изменение логина и пароля Grafana

## Быстрое изменение учётных данных

Используйте автоматический скрипт:

```bash
chmod +x change-grafana-credentials.sh
./change-grafana-credentials.sh
```

Скрипт:
- Запросит новый логин и пароль
- Обновит `.env` файл
- Предложит перезапустить Grafana с новыми данными

## Ручное изменение через .env

1. Отредактируйте `.env` файл:
```bash
nano .env
```

2. Измените переменные:
```env
GRAFANA_USER=ваш_новый_логин
GRAFANA_PASSWORD=ваш_новый_пароль
```

3. Если Grafana уже запущена, перезапустите с полным сбросом:
```bash
# Остановите Grafana
docker-compose -f docker-compose.yml -f docker-compose.monitoring.yml stop grafana

# Удалите данные (это применит новые учётные данные из .env)
docker volume rm bot_ai_cursor_grafana-data

# Запустите заново
docker-compose -f docker-compose.yml -f docker-compose.monitoring.yml up -d grafana
```

## Проблема: пароль не подходит

Если пароль Grafana не подходит, это может быть из-за:
1. Grafana уже была запущена с другим паролем (пароль сохраняется в базе данных)
2. Переменная `GRAFANA_PASSWORD` в `.env` не была применена
3. Пароль был изменён вручную через веб-интерфейс

## Решение 1: Автоматический сброс (рекомендуется)

Используйте скрипт для автоматического сброса:

```bash
chmod +x reset-grafana-password.sh
./reset-grafana-password.sh
```

Скрипт автоматически:
- Проверит настройки из `.env`
- Попытается сбросить пароль через `grafana-cli`
- Если не получится, сбросит через SQL базу данных

## Решение 2: Полный сброс (удаление данных)

Если первый метод не помог, полностью удалите данные Grafana:

```bash
# 1. Остановите Grafana
docker-compose -f docker-compose.yml -f docker-compose.monitoring.yml stop grafana

# 2. Удалите volume с данными
docker volume rm bot_ai_cursor_grafana-data

# 3. Убедитесь, что в .env правильный пароль
nano .env
# Проверьте: GRAFANA_PASSWORD=ваш_пароль

# 4. Запустите Grafana заново
docker-compose -f docker-compose.yml -f docker-compose.monitoring.yml up -d grafana
```

**⚠️ ВНИМАНИЕ:** Это удалит все настройки, дашборды и пользователей Grafana!

## Решение 3: Ручной сброс через контейнер

```bash
# 1. Войдите в контейнер Grafana
docker exec -it grafana bash

# 2. Сбросьте пароль
grafana-cli admin reset-admin-password новый_пароль

# 3. Выйдите из контейнера
exit
```

## Решение 4: Через SQL базу данных

```bash
# 1. Остановите Grafana
docker-compose -f docker-compose.yml -f docker-compose.monitoring.yml stop grafana

# 2. Подключитесь к базе данных
docker run --rm -it \
    -v bot_ai_cursor_grafana-data:/var/lib/grafana \
    grafana/grafana:latest \
    sh

# Внутри контейнера:
cd /var/lib/grafana
sqlite3 grafana.db

# В SQLite:
UPDATE user SET password = '59acf18b94d7eb0694c61e60ce44c110c7a683ac6a8f09580d626f90f4a242000746579358d77dd9e82e111bb673e234' WHERE login = 'admin';
.exit

# 3. Запустите Grafana
docker-compose -f docker-compose.yml -f docker-compose.monitoring.yml up -d grafana
```

Пароль по умолчанию после этого будет: `admin`

## Проверка текущего пароля

Проверьте, какой пароль установлен в `.env`:

```bash
grep GRAFANA_PASSWORD .env
```

Убедитесь, что переменная установлена правильно:

```env
GRAFANA_USER=admin
GRAFANA_PASSWORD=ваш_пароль_здесь
```

## Установка нового пароля

1. Отредактируйте `.env`:
```bash
nano .env
```

2. Установите нужный пароль:
```env
GRAFANA_PASSWORD=ваш_новый_безопасный_пароль
```

3. Перезапустите Grafana:
```bash
docker-compose -f docker-compose.yml -f docker-compose.monitoring.yml restart grafana
```

**⚠️ Важно:** Если Grafana уже была запущена ранее, пароль из `.env` не применится автоматически. Нужно использовать один из методов сброса выше.

## Проверка после сброса

1. Откройте Grafana: `http://ваш_ip:3000`
2. Войдите с новыми учётными данными:
   - Логин: `admin` (или из `GRAFANA_USER`)
   - Пароль: из `GRAFANA_PASSWORD` в `.env`

## Полезные команды

```bash
# Проверка переменных окружения Grafana
docker exec grafana env | grep GF_SECURITY

# Просмотр логов Grafana
docker-compose -f docker-compose.yml -f docker-compose.monitoring.yml logs grafana

# Проверка статуса контейнера
docker-compose -f docker-compose.yml -f docker-compose.monitoring.yml ps grafana
```

