# Инструкция по публикации проекта на GitHub

## Шаг 1: Установка Git

Если Git не установлен, скачайте его с официального сайта:
- **Windows**: https://git-scm.com/download/win
- После установки перезапустите терминал

## Шаг 2: Настройка Git (если ещё не настроен)

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш.email@example.com"
```

## Шаг 3: Инициализация репозитория

Выполните в папке проекта:

```bash
# Инициализация git репозитория
git init

# Добавление всех файлов (кроме тех, что в .gitignore)
git add .

# Создание первого коммита
git commit -m "Initial commit: Telegram bot for workout tracking"
```

## Шаг 4: Создание репозитория на GitHub

1. Перейдите на https://github.com
2. Войдите в свой аккаунт (или создайте новый)
3. Нажмите кнопку **"New"** или **"+"** → **"New repository"**
4. Заполните:
   - **Repository name**: `bot_ai_cursor` (или другое название)
   - **Description**: "Telegram bot for tracking strength training workouts"
   - Выберите **Public** или **Private**
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license" (у нас уже есть эти файлы)
5. Нажмите **"Create repository"**

## Шаг 5: Подключение локального репозитория к GitHub

GitHub покажет инструкции. Выполните (замените `YOUR_USERNAME` на ваш GitHub username):

```bash
# Добавление удалённого репозитория
git remote add origin https://github.com/YOUR_USERNAME/bot_ai_cursor.git

# Переименование основной ветки в main (если нужно)
git branch -M main

# Отправка кода на GitHub
git push -u origin main
```

Если GitHub попросит авторизацию:
- Используйте **Personal Access Token** вместо пароля
- Создайте токен: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token
- Выберите scope: `repo` (полный доступ к репозиториям)

## Готово! 🎉

Ваш проект теперь на GitHub. Вы можете:
- Просматривать код на сайте GitHub
- Клонировать репозиторий на другие компьютеры
- Приглашать других разработчиков к сотрудничеству

## Полезные команды для дальнейшей работы

```bash
# Проверить статус изменений
git status

# Добавить изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Отправить изменения на GitHub
git push

# Получить изменения с GitHub
git pull
```


